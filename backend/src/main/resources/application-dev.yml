spring:
  config:
    activate:
      on-profile: dev
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB
logging:
  level:
    com.blog.cms: DEBUG

blog:
  upload-dir: /data/uploads
