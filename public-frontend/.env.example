# Server-side base URL Next uses to reach the Spring Boot backend (SSR fetches).
# In docker-compose this is the service name; locally it's the host port.
BLOG_API_URL=http://localhost:8080

# Public canonical site URL (used for canonical/OG/sitemap absolute URLs).
NEXT_PUBLIC_SITE_URL=https://news.luuhailong.com
